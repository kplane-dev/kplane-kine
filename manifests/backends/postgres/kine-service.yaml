apiVersion: v1
kind: Service
metadata:
  name: kine
  namespace: kine-pg
spec:
  selector:
    app: kine
  ports:
    - name: etcd
      port: 2379
      targetPort: etcd
    - name: metrics
      port: 8080
      targetPort: metrics
