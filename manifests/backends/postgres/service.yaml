apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: kine-pg
spec:
  clusterIP: None  # headless — postgres has one replica, addressed by name
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
